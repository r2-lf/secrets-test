name: Secrets Test

on:
  pull_request:

jobs:
  secrets-test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo secret
        run: echo "secret = " ${{ secrets.MYSECRET }}
        run: echo ${{ secrets.MYSECRET }} | base64

